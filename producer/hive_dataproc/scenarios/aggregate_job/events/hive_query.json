{
  "eventType": "COMPLETE",
  "job": {
    "namespace": "default",
    "name": "createtable_as_select.default.monthly_transaction_summary"
  },
  "run": {
    "facets": {
      "hive_query": {
        "queryId": "{{ match(result, 'hive_.*') }}",
        "operationName": "CREATETABLE_AS_SELECT"
      }
    }
  }
}