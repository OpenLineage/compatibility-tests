{
  "eventType": "COMPLETE",
  "job": {
    "namespace": "default",
    "name": "{{ match(result, 'big_query_to_delta_on_gcs.append_data_exec_v1.spark_catalog_default_e2e_delta_table_.*') }}"
  },
  "run": {
    "facets": {
      "spark_properties": {
        "properties": {
          "spark.master": "yarn",
          "spark.app.name": "BigQuery to Delta on GCS"
        }
      }
    }
  }
}