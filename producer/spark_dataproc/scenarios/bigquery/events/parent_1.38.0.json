{
  "eventType": "COMPLETE",
  "job": {
    "namespace": "default",
    "name": "{{ match(result, 'writing_to_big_query.execute_save_into_data_source_command.e2e_dataset_wordcount_output_.*') }}"
  },
  "run": {
    "facets": {
      "parent": {
        "run": {
          "runId": "{{ is_uuid(result) }}"
        },
        "job": {
          "namespace": "default",
          "name": "writing_to_big_query"
        },
        "root": {
          "run": {
            "runId": "{{ is_uuid(result) }}"
          },
          "job": {
            "namespace": "default",
            "name": "Writing to BigQuery"
          }
        }
      }
    }
  }
}