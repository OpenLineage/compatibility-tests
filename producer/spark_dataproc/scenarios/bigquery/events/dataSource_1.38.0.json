{
  "eventType": "COMPLETE",
  "job": {
    "namespace": "default",
    "name": "{{ match(result, 'writing_to_big_query.execute_save_into_data_source_command.e2e_dataset_.*_wordcount_output') }}"
  },
  "inputs": [
    {
      "namespace": "bigquery",
      "name": "bigquery-public-data.samples.shakespeare",
      "facets": {
        "dataSource": {
          "name": "bigquery",
          "uri": "bigquery"
        }
      }
    }
  ],
  "outputs": [
    {
      "namespace": "bigquery",
      "name": "{{ match(result, 'gcp-open-lineage-testing.e2e_dataset.*\\.wordcount_output') }}",
      "facets": {
        "dataSource": {
          "name": "bigquery",
          "uri": "bigquery"
        }
      }
    }
  ]
}