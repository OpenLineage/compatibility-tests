{
  "eventTime": "{{ is_datetime(result) }}",
  "eventType": "COMPLETE",
  "run": {
    "runId": "{{ is_uuid(result) }}"
  },
  "job": {
    "namespace": "default",
    "name": "spark_spanner_example"
  }
}