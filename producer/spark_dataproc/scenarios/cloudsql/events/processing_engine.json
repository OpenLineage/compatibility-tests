{
    "eventType": "COMPLETE",
    "job": {
      "namespace": "default",
      "name": "spark_cloud_sql_example"
    },
    "run": {
      "facets": {
        "processing_engine": {
            "version": "{{ any(result) }}",
            "name": "spark",
            "openlineageAdapterVersion": "{{ any(result) }}"
        }
      }
    }
}
